# Plotting method for `irp_prediction_domain` objects

Plotting method for `irp_prediction_domain` objects

## Usage

``` r
# S3 method for class 'irp_prediction_domain'
plot(x, ...)
```

## Arguments

- x:

  An object of class `irp_prediction_domain`.

- ...:

  Further parameters; passed to
  [`ggplot2::geom_ribbon`](https://ggplot2.tidyverse.org/reference/geom_ribbon.html).

## Value

A `ggplot` object.
